// Secrets.xcconfig 템플릿. 실제 Secrets.xcconfig는 gitignore 되어 있다.
// 로컬에서 `cp Secrets.xcconfig.example Secrets.xcconfig` 후 값을 채워 사용한다.

// xcconfig의 `//`는 주석이므로 URL scheme은 `/$()/`로 escape 한다.
API_BASE_URL_DEBUG = https:/$()/dev.rundiary.app
API_BASE_URL_RELEASE = https:/$()/rundiary.app
